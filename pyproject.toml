[project]
name = "vertex-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "gradio>=6.28.0",
    "ollama>=0.6.2",
    "pandas>=3.0.6",
]

[project.scripts]
vertex-ai = "vertex_ai:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
